# Minimal Airflow setup for local DAG development
# Uses SQLite + SequentialExecutor (no Postgres needed)

services:
  airflow:
    image: apache/airflow:2.7.0-python3.11
    environment:
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: sqlite:////opt/airflow/airflow.db
      AIRFLOW__CORE__EXECUTOR: SequentialExecutor
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__API__AUTH_BACKENDS: "airflow.api.auth.backend.session,airflow.api.auth.backend.basic_auth"

      GOOGLE_CLOUD_PROJECT: ${PROJECT_ID}
      GCS_BUCKET: ${GCS_BUCKET}

      # SMTP (Gmail)
      AIRFLOW__SMTP__SMTP_HOST: smtp.gmail.com
      AIRFLOW__SMTP__SMTP_PORT: 587
      AIRFLOW__SMTP__SMTP_STARTTLS: "True"
      AIRFLOW__SMTP__SMTP_SSL: "False"
      AIRFLOW__SMTP__SMTP_USER: ${SMTP_MAIL_FROM}
      AIRFLOW__SMTP__SMTP_PASSWORD: ${SMTP_PASSWORD}
      AIRFLOW__SMTP__SMTP_MAIL_FROM: ${SMTP_MAIL_FROM}

    volumes:
      - ./dags:/opt/airflow/dags
      - ./data:/opt/airflow/data
      - ./logs:/opt/airflow/logs
      - ./airflow_home:/opt/airflow
      - ~/.config/gcloud:/home/airflow/.config/gcloud:ro

    ports:
      - "8080:8080"

    command: >
      bash -euc "
        mkdir -p /opt/airflow/secrets
        rm -f /opt/airflow/*.pid /opt/airflow/airflow-*.pid || true
        airflow db migrate || airflow db init
        airflow users create --username admin --password admin --firstname Admin --lastname User --role Admin --email admin@example.com || true
        airflow scheduler & exec airflow webserver
      "
